const incorreto_popup = document.getElementById('informacoes_incorretas');
let position = -50;
const speed = 2; // Ajuste a velocidade aqui (número de pixels por intervalo)


function abaixar_popup() {
    const interval = setInterval(function () {
        position += speed;
        rectangle.style.top = position + "px";
    
        if (position >= 30) {
            console.log
}

abaixar_popup();